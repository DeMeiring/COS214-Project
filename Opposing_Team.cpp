#include "Opposing_Team.h"

vector <tuple<string, Driver*, Engineering*>> Opposing_Team::GenerateTeam(string DriverName1, string DriverName2, int difficulty) {
    // create two drivers based on difficulty -> [1, 100]
}